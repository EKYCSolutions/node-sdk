{
  "name": "ekycsolutions",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^17.0.21",
    "jest": "^27.5.1",
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "ts-jest": "^27.1.3",
    "typescript": "^4.6.2"
  },
  "scripts": {
    "clean": "lerna run clean",
    "publish": "yarn clean && lerna run tsc && lerna publish"
  }
}
